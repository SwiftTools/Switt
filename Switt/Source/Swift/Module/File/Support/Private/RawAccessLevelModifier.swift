enum RawAccessLevelModifier: String {
    case Public = "public"
    case Internal = "internal"
    case Private = "private"
    case PublicSet = "public(set)"
    case InternalSet = "internal(set)"
    case PrivateSet = "private(set)"
}